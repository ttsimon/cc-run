# ccr chain（v0.3）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让 ccr 能按一份 yaml 把多个不同 provider 的 `claude -p` 串成流水线（规划→实现→审查），段间共享 git 工作目录、产出写文件交棒，默认人在环里放行、`--auto` 一条道跑到黑。

**Architecture:** 新增 `internal/chain` 子系统：纯逻辑（schema 解析、prompt 模板、命令构造、findings/判定）全单测；段执行复用 `internal/launcher` 的「纯构造 + 注入式 struct + helper-process 集成测试」模式；编排器顺序跑各段、把上段 stdout 注入下段、按 review 判定与放行决策推进。安全靠 git worktree 隔离 + 每段 `--allowedTools` 白名单 + PreToolUse 钩子调 `ccr __chain_guard` 命中黑名单即拦。

**Tech Stack:** Go（CGO_ENABLED=0）；yaml 用 `gopkg.in/yaml.v3`（新依赖）；复用 `internal/registry`(按名解析 profile)、`internal/launcher`(ComposeEnv)、`internal/profile`；TUI 放行复用 `charmbracelet/huh`。

---

## 阅前必读：分阶段交付 + 与 claude CLI 的集成边界

**本计划分 6 个阶段（A–F），每阶段跑完都是可用的里程碑**，建议逐阶段实现 + 合并：

- **Phase A**：两个独立小依赖（per-profile 默认参数、`ccr doctor`）。最简单，先做。
- **Phase B**：chain 核心 —— 跑通线性链 `ccr chain <file> --auto`。**用假 claude（helper-process）即可全测**，不依赖真 claude。
- **Phase C**：人在环里 —— 段间放行 `⏸`（放行/改/跳过/退出）。
- **Phase D**：审查段 findings + 判定 + 可选修复段。
- **Phase E**：安全 —— allow-tools、PreToolUse 黑名单钩子、git worktree 隔离。
- **Phase F**：`ccr chain init` + 内置模板 + 文档。

**与 claude CLI 的集成边界（重要）**：段执行最终是拉起 `claude -p`，用到这些 claude 旗标——`-p`（headless 打印模式）、`--allowedTools`、`--add-dir`、`--permission-mode`、`--settings <path>`（注入 PreToolUse 钩子）、`--output-format text`。本计划据当前 Claude Code CLI 的旗标编写；**每个真正 exec claude 的任务都带一步「`claude --help` 确认这些旗标在你装的版本里存在，名字若不同则改 `segmentFlags()` 里的常量」**。命令构造是纯函数、单测覆盖；exec 用 helper-process 假 claude 测，故绝大多数逻辑无需真 claude 即可验证。

**约定**：Conventional Commits（commit-msg 钩子强制），`feat:`/`fix:` 进发布说明。每个 Task 末尾 commit，body 末尾加 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。master 受保护、PR-only，本计划在分支上做。CI 现在只在 PR 上跑。提交前本地至少 `go test ./...` + `gofmt -l` + `go vet ./...`（golangci-lint 由 CI 把关）。

---

## File Structure

```
internal/profile/profile.go      改：Profile 加 Args []string（per-profile 默认参数）
internal/source/parse.go         改：解析 settings 里的 args / 自定义 json 的 args
internal/launcher/launcher.go    改：ClaudeArgs 把 profile.Args 接进去
internal/doctor/doctor.go        新：探测某 profile 的后端可达性（HTTP HEAD/GET）
internal/chain/schema.go         新：Chain/Segment 类型 + Parse + Validate
internal/chain/template.go       新：渲染 {{prev.output}}
internal/chain/runner.go         新：段命令/env 构造（纯）+ RunSegment（注入式 exec，捕获 stdout）
internal/chain/verdict.go        新：审查段 findings/判定 约定的读写
internal/chain/security.go       新：默认黑名单 + 合并 yaml 追加 + 生成 PreToolUse settings.json
internal/chain/worktree.go       新：git worktree 隔离的建/拆
internal/chain/pause.go          新：段间放行 Decision + Pauser 接口 + huh 实现
internal/chain/orchestrate.go    新：Orchestrator —— 顺序跑段、交棒、判定、放行推进
internal/chain/templates/        新：内置 chain yaml 模板（go:embed）
internal/cli/cli.go              改：分发 chain / doctor / __chain_guard 子命令；帮助
go.mod / go.sum                  改：加 gopkg.in/yaml.v3
```

每个文件一个清晰职责；orchestrate.go 只编排、不碰具体 exec 细节（委托 runner）。

---

# Phase A — 独立小依赖

## Task A1: Profile 加 per-profile 默认参数

**Files:**
- Modify: `internal/profile/profile.go`
- Modify: `internal/source/parse.go`
- Modify: `internal/source/parse_test.go`
- Modify: `internal/launcher/launcher.go`
- Modify: `internal/launcher/launcher_test.go`

- [ ] **Step 1: 给 Profile 加字段**

在 `internal/profile/profile.go` 的 `Profile` struct 末尾加一行字段：

```go
	IsCurrent bool              // 仅 cc-switch：是否为当前激活 provider
	Args      []string          // per-profile 默认参数，拉起 claude 时追加在 model 之后、用户 extra 之前
```

- [ ] **Step 2: 写解析的失败测试**

解析入口是 `internal/source/parse.go` 的 `ParseSettingsConfig(name string, src profile.Source, raw string) (profile.Profile, error)`，对应结构体 `settingsConfig{ Model string; Env map[string]string }`（cc-switch 与 customdir 两条路径都走它，一处改即可）。在 `internal/source/parse_test.go` 追加（若文件未 import `profile` 则补上）：

```go
func TestParse_读取per_profile_args(t *testing.T) {
	raw := `{"model":"sonnet","args":["--verbose"],"env":{"ANTHROPIC_BASE_URL":"http://x"}}`
	p, err := ParseSettingsConfig("demo", profile.SourceCustom, raw)
	if err != nil {
		t.Fatal(err)
	}
	if len(p.Args) != 1 || p.Args[0] != "--verbose" {
		t.Errorf("args 未解析: %+v", p.Args)
	}
}
```

- [ ] **Step 3: 跑测试确认失败**

Run: `go test ./internal/source/ -run TestParse_读取per_profile_args`
Expected: FAIL（json 里的 `args` 未被读到，p.Args 为空）

- [ ] **Step 4: 在解析里读 args**

`internal/source/parse.go`：给 `settingsConfig` 加字段 `Args []string \`json:"args"\``，并在 `ParseSettingsConfig` 返回的 `profile.Profile{...}` 里加 `Args: sc.Args`。

- [ ] **Step 5: 跑测试确认通过**

Run: `go test ./internal/source/`
Expected: PASS

- [ ] **Step 6: 让 ClaudeArgs 用上 profile.Args（失败测试先行）**

在 `internal/launcher/launcher_test.go` 追加：

```go
func TestClaudeArgs_接入profile默认参数(t *testing.T) {
	got := ClaudeArgs("sonnet", nil, []string{"--verbose"}, []string{"-p", "hi"})
	if strings.Join(got, " ") != "--model sonnet --verbose -p hi" {
		t.Errorf("默认参数应在 model 之后、extra 之前: %v", got)
	}
}
```

- [ ] **Step 7: 跑测试确认失败（签名不匹配/编译错）**

Run: `go test ./internal/launcher/ -run TestClaudeArgs_接入profile默认参数`
Expected: FAIL（`ClaudeArgs` 目前是 3 参，编译不过）

- [ ] **Step 8: 改 ClaudeArgs 签名加入 profileArgs**

`internal/launcher/launcher.go` 把 `ClaudeArgs` 改为：

```go
// ClaudeArgs 计算传给 claude 的参数：--model（按需）+ profile 默认参数 + 用户 extra。
func ClaudeArgs(model string, profileEnv map[string]string, profileArgs []string, extra []string) []string {
	var args []string
	_, envHasModel := profileEnv["ANTHROPIC_MODEL"]
	if model != "" && !envHasModel {
		args = append(args, "--model", model)
	}
	args = append(args, profileArgs...)
	return append(args, extra...)
}
```

并更新 `Run` 里的调用：`ClaudeArgs(p.Model, p.Env, p.Args, extra)`。同时更新原有 `TestClaudeArgs` 的三处调用为四参（加一个 `nil`）。

- [ ] **Step 9: 跑测试确认通过**

Run: `go test ./internal/launcher/ ./internal/source/ ./internal/profile/`
Expected: PASS

- [ ] **Step 10: 提交**

```bash
git add internal/profile/ internal/source/ internal/launcher/
git commit -m "feat: per-profile default args appended to claude invocation

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task A2: `ccr doctor` —— 后端可达性体检

**Files:**
- Create: `internal/doctor/doctor.go`
- Create: `internal/doctor/doctor_test.go`
- Modify: `internal/cli/cli.go`

- [ ] **Step 1: 写失败测试（用 httptest 起假后端）**

`internal/doctor/doctor_test.go`:

```go
package doctor

import (
	"net/http"
	"net/http/httptest"
	"testing"

	"github.com/ttsimon/cc-run/internal/profile"
)

func TestCheck_可达返回ok(t *testing.T) {
	srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		w.WriteHeader(200)
	}))
	defer srv.Close()
	p := profile.Profile{Name: "x", Env: map[string]string{"ANTHROPIC_BASE_URL": srv.URL}}
	res := Check(p)
	if !res.OK {
		t.Errorf("可达应 OK: %+v", res)
	}
}

func TestCheck_无base_url标记跳过(t *testing.T) {
	p := profile.Profile{Name: "x", Env: map[string]string{}}
	res := Check(p)
	if res.OK || res.Detail == "" {
		t.Errorf("无 BASE_URL 应非 OK 且给原因: %+v", res)
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/doctor/`
Expected: FAIL（包不存在）

- [ ] **Step 3: 写实现**

`internal/doctor/doctor.go`:

```go
// Package doctor 探测某 Profile 的后端是否可达，用于跑 chain 前的体检。
package doctor

import (
	"fmt"
	"net/http"
	"time"

	"github.com/ttsimon/cc-run/internal/profile"
)

// Result 是一次体检结果。
type Result struct {
	Name   string
	OK     bool
	Detail string // 失败原因或 "HTTP 200" 等
}

// httpClient 便于测试替换超时。
var httpClient = &http.Client{Timeout: 5 * time.Second}

// Check 对 p 的 ANTHROPIC_BASE_URL 发一个 GET，2xx/3xx/401/403 都视为「后端在」。
// 401/403 说明端点活着只是鉴权——对「可达性」而言算通。
func Check(p profile.Profile) Result {
	base := p.Env["ANTHROPIC_BASE_URL"]
	if base == "" {
		return Result{Name: p.Name, OK: false, Detail: "无 ANTHROPIC_BASE_URL"}
	}
	resp, err := httpClient.Get(base)
	if err != nil {
		return Result{Name: p.Name, OK: false, Detail: err.Error()}
	}
	defer resp.Body.Close()
	ok := resp.StatusCode < 500
	return Result{Name: p.Name, OK: ok, Detail: fmt.Sprintf("HTTP %d", resp.StatusCode)}
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `go test ./internal/doctor/`
Expected: PASS

- [ ] **Step 5: 接 cli（`ccr doctor [名字]`）**

在 `internal/cli/cli.go` 的子命令 switch 里 `case "edit":` 之后加：

```go
		case "doctor":
			return runDoctor(cfg, args[1:], os.Stdout)
```

并在文件末尾追加（import 加 `"github.com/ttsimon/cc-run/internal/doctor"`）：

```go
// runDoctor: 无参体检全部 profile；带名只检一个。
func runDoctor(cfg config.Config, args []string, out io.Writer) int {
	r, code := buildRegistry(cfg)
	if code != 0 {
		return code
	}
	var targets []profile.Profile
	if len(args) > 0 {
		p, err := r.Resolve(args[0])
		if err != nil {
			fmt.Fprintln(os.Stderr, err)
			return 1
		}
		targets = []profile.Profile{p}
	} else {
		targets = r.List()
	}
	allOK := true
	for _, p := range targets {
		res := doctor.Check(p)
		mark := "✓"
		if !res.OK {
			mark = "✗"
			allOK = false
		}
		fmt.Fprintf(out, "%s %-20s %s\n", mark, res.Name, res.Detail)
	}
	if !allOK {
		return 1
	}
	return 0
}
```

- [ ] **Step 6: 构建 + 冒烟 + 提交**

Run: `go build ./cmd/ccr && go run ./cmd/ccr doctor`
Expected: 列出每个 profile 的可达性（✓/✗）。

```bash
git add internal/doctor/ internal/cli/cli.go
git commit -m "feat: add ccr doctor to check backend reachability

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

# Phase B — chain 核心（线性 + --auto，假 claude 可全测）

## Task B1: 加 yaml 依赖 + chain schema 解析/校验

**Files:**
- Create: `internal/chain/schema.go`
- Create: `internal/chain/schema_test.go`
- Modify: `go.mod`, `go.sum`

- [ ] **Step 1: 加依赖**

Run: `go get gopkg.in/yaml.v3@latest`
Expected: go.mod 出现 `gopkg.in/yaml.v3`。

- [ ] **Step 2: 写失败测试**

`internal/chain/schema_test.go`:

```go
package chain

import (
	"strings"
	"testing"
)

const sampleYAML = `
name: plan-impl-review
isolate: true
segments:
  - name: plan
    profile: cc-switch:strong
    prompt: 把目标拆成 docs/plans/x.md
    allow_tools: [Read, Write]
  - name: implement
    profile: custom:cheap
    prompt: 读 {{prev.output}} 照着实现
  - name: review
    profile: cc-switch:other
    prompt: 审查改动
    review: true
`

func TestParse_合法链(t *testing.T) {
	c, err := Parse([]byte(sampleYAML))
	if err != nil {
		t.Fatal(err)
	}
	if c.Name != "plan-impl-review" || !c.Isolate || len(c.Segments) != 3 {
		t.Fatalf("解析不对: %+v", c)
	}
	if c.Segments[0].Profile != "cc-switch:strong" || len(c.Segments[0].AllowTools) != 2 {
		t.Errorf("段0 不对: %+v", c.Segments[0])
	}
	if !c.Segments[2].Review {
		t.Errorf("段2 应为 review")
	}
}

func TestValidate_空段报错(t *testing.T) {
	_, err := Parse([]byte("name: x\nsegments: []\n"))
	if err == nil || !strings.Contains(err.Error(), "段") {
		t.Errorf("空 segments 应报错: %v", err)
	}
}

func TestValidate_段缺profile报错(t *testing.T) {
	_, err := Parse([]byte("name: x\nsegments:\n  - name: a\n    prompt: hi\n"))
	if err == nil {
		t.Error("缺 profile 应报错")
	}
}
```

- [ ] **Step 3: 跑测试确认失败**

Run: `go test ./internal/chain/`
Expected: FAIL（包/类型不存在）

- [ ] **Step 4: 写实现**

`internal/chain/schema.go`:

```go
// Package chain 把一份 yaml 描述的多后端流水线解析、校验并编排执行。
package chain

import (
	"fmt"

	"gopkg.in/yaml.v3"
)

// Chain 是一条流水线。
type Chain struct {
	Name     string    `yaml:"name"`
	Isolate  bool      `yaml:"isolate"`  // true 则在临时 git worktree 跑，可整体回滚
	Workdir  string    `yaml:"workdir"`  // 工作目录，空=当前目录
	Segments []Segment `yaml:"segments"`
}

// Segment 是一段：用某 provider 跑一次无头 claude。
type Segment struct {
	Name         string   `yaml:"name"`
	Profile      string   `yaml:"profile"`       // ccr profile 查询名（registry 解析）
	Prompt       string   `yaml:"prompt"`        // 可含 {{prev.output}}
	AllowTools   []string `yaml:"allow_tools"`   // claude --allowedTools
	DenyCommands []string `yaml:"deny_commands"` // 追加到内置命令黑名单
	Review       bool     `yaml:"review"`        // 该段产出 findings + 判定
	Optional     bool     `yaml:"optional"`      // 仅在放行点用户选择时才跑（如修复段）
}

// Parse 解析并校验一条链。
func Parse(data []byte) (Chain, error) {
	var c Chain
	if err := yaml.Unmarshal(data, &c); err != nil {
		return Chain{}, fmt.Errorf("解析 chain yaml 失败: %w", err)
	}
	if err := c.Validate(); err != nil {
		return Chain{}, err
	}
	return c, nil
}

// Validate 校验基本约束。
func (c Chain) Validate() error {
	if len(c.Segments) == 0 {
		return fmt.Errorf("chain 至少要有一个段（segments 为空）")
	}
	for i, s := range c.Segments {
		if s.Profile == "" {
			return fmt.Errorf("段 #%d(%q) 缺 profile", i, s.Name)
		}
		if s.Prompt == "" {
			return fmt.Errorf("段 #%d(%q) 缺 prompt", i, s.Name)
		}
	}
	return nil
}
```

- [ ] **Step 5: 跑测试确认通过 + 提交**

Run: `go test ./internal/chain/`
Expected: PASS

```bash
git add internal/chain/schema.go internal/chain/schema_test.go go.mod go.sum
git commit -m "feat: add chain yaml schema parsing and validation

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task B2: prompt 模板渲染（{{prev.output}}）

**Files:**
- Create: `internal/chain/template.go`
- Create: `internal/chain/template_test.go`

- [ ] **Step 1: 写失败测试**

`internal/chain/template_test.go`:

```go
package chain

import "testing"

func TestRender_替换prev输出(t *testing.T) {
	got := Render("读 {{prev.output}} 照做", "计划在 docs/plans/x.md")
	if got != "读 计划在 docs/plans/x.md 照做" {
		t.Errorf("got %q", got)
	}
}

func TestRender_容忍空格变体(t *testing.T) {
	got := Render("X {{ prev.output }} Y", "Z")
	if got != "X Z Y" {
		t.Errorf("应容忍花括号内空格: %q", got)
	}
}

func TestRender_无占位原样返回(t *testing.T) {
	if Render("没有占位", "忽略") != "没有占位" {
		t.Error("无占位应原样")
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/chain/ -run TestRender`
Expected: FAIL（Render 未定义）

- [ ] **Step 3: 写实现**

`internal/chain/template.go`:

```go
package chain

import "strings"

// Render 把 prompt 里的 {{prev.output}}（容忍内部空格）替换为上一段输出。
// 故意只支持这一个占位符，不引入 text/template，保持简单可预期。
func Render(prompt, prevOutput string) string {
	out := prompt
	for _, token := range []string{"{{prev.output}}", "{{ prev.output }}"} {
		out = strings.ReplaceAll(out, token, prevOutput)
	}
	return out
}
```

- [ ] **Step 4: 跑测试确认通过 + 提交**

Run: `go test ./internal/chain/ -run TestRender`
Expected: PASS

```bash
git add internal/chain/template.go internal/chain/template_test.go
git commit -m "feat: add chain prompt templating for prev.output

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task B3: 段命令构造（纯函数）

**Files:**
- Create: `internal/chain/runner.go`
- Create: `internal/chain/runner_test.go`

- [ ] **Step 1: 写失败测试**

`internal/chain/runner_test.go`（先只测纯构造）:

```go
package chain

import (
	"strings"
	"testing"
)

func TestSegmentArgs_基本(t *testing.T) {
	got := SegmentArgs("照做", []string{"Read", "Write"}, "/tmp/wd", "")
	j := strings.Join(got, " ")
	// 必含：-p、prompt、--allowedTools 列表、--add-dir、--output-format text
	for _, want := range []string{"-p", "照做", "--allowedTools", "Read,Write", "--add-dir", "/tmp/wd", "--output-format", "text"} {
		if !strings.Contains(j, want) {
			t.Errorf("缺 %q: %v", want, got)
		}
	}
}

func TestSegmentArgs_带settings(t *testing.T) {
	got := SegmentArgs("x", nil, "/wd", "/tmp/settings.json")
	if !strings.Contains(strings.Join(got, " "), "--settings /tmp/settings.json") {
		t.Errorf("应含 --settings: %v", got)
	}
}

func TestSegmentArgs_无allowtools不加旗标(t *testing.T) {
	got := SegmentArgs("x", nil, "/wd", "")
	if strings.Contains(strings.Join(got, " "), "--allowedTools") {
		t.Errorf("无 allow_tools 不应加 --allowedTools: %v", got)
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/chain/ -run TestSegmentArgs`
Expected: FAIL

- [ ] **Step 3: 写实现（命令构造，纯函数）**

`internal/chain/runner.go`（先只写构造部分；exec 在 B4 加）:

```go
package chain

import "strings"

// SegmentArgs 构造传给 claude 的参数：headless 打印模式 + 工具白名单 + 目录范围 + 钩子设置。
// settingsPath 为空时不加 --settings；allowTools 为空时不加 --allowedTools。
//
// ⚠️ 集成边界：这些旗标名按当前 Claude Code CLI 编写。实现时跑一次
// `claude --help` 确认 -p / --allowedTools / --add-dir / --output-format / --settings 存在，
// 版本不同则只改这里的常量字符串。
func SegmentArgs(prompt string, allowTools []string, workdir, settingsPath string) []string {
	args := []string{"-p", prompt, "--output-format", "text", "--add-dir", workdir}
	if len(allowTools) > 0 {
		args = append(args, "--allowedTools", strings.Join(allowTools, ","))
	}
	if settingsPath != "" {
		args = append(args, "--settings", settingsPath)
	}
	return args
}
```

- [ ] **Step 4: 跑测试确认通过 + 提交**

Run: `go test ./internal/chain/ -run TestSegmentArgs`
Expected: PASS

```bash
git add internal/chain/runner.go internal/chain/runner_test.go
git commit -m "feat: build claude segment command args

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task B4: 段执行（注入式 exec + 捕获 stdout，helper-process 测）

**Files:**
- Modify: `internal/chain/runner.go`
- Modify: `internal/chain/runner_test.go`

- [ ] **Step 1: 写 helper-process 失败测试**

在 `internal/chain/runner_test.go` 追加（仿 `internal/launcher/launcher_test.go` 的 helper-process 模式）：

```go
import (
	"bytes"
	"os"
	// 与上方 import 合并
)

// TestHelperProcess 充当假 claude：把 -p 后的 prompt 原样打印到 stdout，并打印一个 env 值。
func TestHelperProcess(t *testing.T) {
	if os.Getenv("GO_WANT_HELPER_PROCESS") != "1" {
		return
	}
	args := os.Args
	var prompt string
	for i, a := range args {
		if a == "-p" && i+1 < len(args) {
			prompt = args[i+1]
		}
	}
	os.Stdout.WriteString("PROMPT=" + prompt + "\nBASE=" + os.Getenv("ANTHROPIC_BASE_URL"))
	os.Exit(0)
}

func TestRunSegment_捕获stdout并注入env(t *testing.T) {
	r := NewRunner()
	r.ClaudePath = os.Args[0]
	r.Environ = func() []string { return append(os.Environ(), "GO_WANT_HELPER_PROCESS=1") }
	var errOut bytes.Buffer
	r.Stderr = &errOut

	out, code, err := r.RunSegment(runSpec{
		Prompt:     "你好",
		Env:        map[string]string{"ANTHROPIC_BASE_URL": "https://injected"},
		ExtraArgs:  []string{"-test.run=TestHelperProcess"},
	})
	if err != nil {
		t.Fatal(err)
	}
	if code != 0 {
		t.Errorf("退出码 %d", code)
	}
	if !strings.Contains(out, "PROMPT=你好") || !strings.Contains(out, "BASE=https://injected") {
		t.Errorf("应捕获到 stdout 并注入 env: %q", out)
	}
}
```

> 说明：`runSpec` 是给 RunSegment 的入参结构（下step 定义）。`ExtraArgs` 仅测试用——把测试二进制重新指向 helper 函数；真实运行时为空。

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/chain/ -run TestRunSegment`
Expected: FAIL（NewRunner/RunSegment/runSpec 未定义）

- [ ] **Step 3: 写实现（Runner + RunSegment）**

在 `internal/chain/runner.go` 追加（仿 launcher.Launcher，复用 `launcher.ComposeEnv`）：

```go
import (
	"bytes"
	"errors"
	"fmt"
	"io"
	"os"
	"os/exec"
	"strings"

	"github.com/ttsimon/cc-run/internal/launcher"
)

// runSpec 是跑一段所需的全部输入。
type runSpec struct {
	Prompt       string
	AllowTools   []string
	Workdir      string
	SettingsPath string
	Env          map[string]string // 已解析 profile 的 env
	ExtraArgs    []string          // 仅测试注入；真实运行为空
}

// Runner 用某 profile 拉起一次无头 claude，捕获其 stdout 作为本段产出。
type Runner struct {
	ClaudePath string
	LookPath   func(string) (string, error)
	Environ    func() []string
	Stderr     io.Writer
}

// NewRunner 返回带默认值的 Runner。
func NewRunner() *Runner {
	return &Runner{
		LookPath: exec.LookPath,
		Environ:  os.Environ,
		Stderr:   os.Stderr,
	}
}

// RunSegment 跑一段，返回 (stdout 文本, 退出码, error)。
// 仅当无法启动 claude 时返回 error；子进程非 0 退出通过 code 透传。
func (r *Runner) RunSegment(spec runSpec) (string, int, error) {
	path := r.ClaudePath
	if path == "" {
		found, err := r.LookPath("claude")
		if err != nil {
			return "", -1, fmt.Errorf("找不到 claude 可执行文件：%w", err)
		}
		path = found
	}

	args := SegmentArgs(spec.Prompt, spec.AllowTools, spec.Workdir, spec.SettingsPath)
	args = append(args, spec.ExtraArgs...)

	cmd := exec.Command(path, args...)
	cmd.Env = launcher.ComposeEnv(r.Environ(), spec.Env)
	var out bytes.Buffer
	cmd.Stdout = &out
	cmd.Stderr = r.Stderr

	err := cmd.Run()
	if err == nil {
		return out.String(), 0, nil
	}
	var exitErr *exec.ExitError
	if errors.As(err, &exitErr) {
		return out.String(), exitErr.ExitCode(), nil
	}
	return out.String(), -1, err
}
```
> 注意 import：`strings` 已被 `SegmentArgs`（B3）用到，本文件追加的这段又用到 `bytes`/`errors`/`fmt`/`io`/`os`/`os/exec` 与 `launcher`——把它们并入 runner.go 顶部已有的 import 块，别重复声明、别留未用项。

- [ ] **Step 4: 跑测试确认通过 + 提交**

Run: `go test ./internal/chain/`
Expected: PASS

```bash
git add internal/chain/runner.go internal/chain/runner_test.go
git commit -m "feat: exec claude segment and capture stdout

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task B5: 编排器（顺序跑段 + 交棒 + --auto）

**Files:**
- Create: `internal/chain/orchestrate.go`
- Create: `internal/chain/orchestrate_test.go`

本任务先做**最小编排**：解析每段 profile → 渲染 prompt（注入上段 stdout）→ 跑 → 上段输出喂下段。暂不做放行/审查判定/安全（后续 Phase）。`isolate`/worktree 也暂不接，跑在 `Workdir`（空=当前目录）。

- [ ] **Step 1: 写失败测试（用可注入的 runSegment 桩，免真 claude）**

`internal/chain/orchestrate_test.go`:

```go
package chain

import (
	"strings"
	"testing"

	"github.com/ttsimon/cc-run/internal/profile"
	"github.com/ttsimon/cc-run/internal/registry"
)

func testReg() *registry.Registry {
	return registry.New([]profile.Profile{
		{Name: "strong", Source: profile.SourceCCSwitch, Env: map[string]string{"ANTHROPIC_BASE_URL": "http://a"}},
		{Name: "cheap", Source: profile.SourceCustom, Env: map[string]string{"ANTHROPIC_BASE_URL": "http://b"}},
	})
}

func TestOrchestrate_顺序跑并交棒(t *testing.T) {
	c := Chain{
		Name: "t",
		Segments: []Segment{
			{Name: "a", Profile: "strong", Prompt: "first"},
			{Name: "b", Profile: "cheap", Prompt: "用了上段: {{prev.output}}"},
		},
	}
	var seenPrompts []string
	o := NewOrchestrator(testReg())
	o.Auto = true
	// 注入桩：每段返回 "<name>-out"，并记录它收到的 prompt
	o.runSegment = func(spec runSpec, seg Segment) (string, int, error) {
		seenPrompts = append(seenPrompts, spec.Prompt)
		return seg.Name + "-out", 0, nil
	}
	if err := o.Run(c); err != nil {
		t.Fatal(err)
	}
	if len(seenPrompts) != 2 {
		t.Fatalf("应跑 2 段, got %d", len(seenPrompts))
	}
	if seenPrompts[0] != "first" {
		t.Errorf("段0 prompt 错: %q", seenPrompts[0])
	}
	if !strings.Contains(seenPrompts[1], "a-out") {
		t.Errorf("段1 应注入上段输出: %q", seenPrompts[1])
	}
}

func TestOrchestrate_未知profile报错(t *testing.T) {
	c := Chain{Segments: []Segment{{Name: "a", Profile: "nope", Prompt: "x"}}}
	o := NewOrchestrator(testReg())
	o.Auto = true
	o.runSegment = func(spec runSpec, seg Segment) (string, int, error) { return "", 0, nil }
	if err := o.Run(c); err == nil {
		t.Error("未知 profile 应报错")
	}
}

func TestOrchestrate_段非零退出即停(t *testing.T) {
	c := Chain{Segments: []Segment{
		{Name: "a", Profile: "strong", Prompt: "x"},
		{Name: "b", Profile: "cheap", Prompt: "y"},
	}}
	calls := 0
	o := NewOrchestrator(testReg())
	o.Auto = true
	o.runSegment = func(spec runSpec, seg Segment) (string, int, error) {
		calls++
		return "", 3, nil // 非 0 退出
	}
	if err := o.Run(c); err == nil {
		t.Error("段非 0 退出应中止并报错")
	}
	if calls != 1 {
		t.Errorf("应在第一段失败后停, calls=%d", calls)
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/chain/ -run TestOrchestrate`
Expected: FAIL

- [ ] **Step 3: 写实现**

`internal/chain/orchestrate.go`:

```go
package chain

import (
	"fmt"

	"github.com/ttsimon/cc-run/internal/registry"
)

// Orchestrator 顺序执行一条链的各段。
type Orchestrator struct {
	reg  *registry.Registry
	Auto bool // true=不停顿一条道跑到黑（Phase C 才有停顿）

	// runSegment 可注入以便测试；默认走真实 Runner。
	runSegment func(spec runSpec, seg Segment) (string, int, error)
}

// NewOrchestrator 返回默认编排器（真实跑 claude）。
func NewOrchestrator(reg *registry.Registry) *Orchestrator {
	o := &Orchestrator{reg: reg}
	runner := NewRunner()
	o.runSegment = func(spec runSpec, seg Segment) (string, int, error) {
		return runner.RunSegment(spec)
	}
	return o
}

// Run 顺序跑完整条链。上段 stdout 注入下段 prompt 的 {{prev.output}}。
func (o *Orchestrator) Run(c Chain) error {
	workdir := c.Workdir
	if workdir == "" {
		workdir = "."
	}
	var prev string
	for i, seg := range c.Segments {
		p, err := o.reg.Resolve(seg.Profile)
		if err != nil {
			return fmt.Errorf("段 #%d(%q) 的 profile 解析失败: %w", i, seg.Name, err)
		}
		spec := runSpec{
			Prompt:     Render(seg.Prompt, prev),
			AllowTools: seg.AllowTools,
			Workdir:    workdir,
			Env:        p.Env,
		}
		out, code, err := o.runSegment(spec, seg)
		if err != nil {
			return fmt.Errorf("段 #%d(%q) 启动失败: %w", i, seg.Name, err)
		}
		if code != 0 {
			return fmt.Errorf("段 #%d(%q) 非 0 退出（%d），中止", i, seg.Name, code)
		}
		prev = out
	}
	return nil
}
```

- [ ] **Step 4: 跑测试确认通过 + 提交**

Run: `go test ./internal/chain/`
Expected: PASS

```bash
git add internal/chain/orchestrate.go internal/chain/orchestrate_test.go
git commit -m "feat: orchestrate chain segments sequentially with handoff

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task B6: `ccr chain <file> --auto` 接入 cli

**Files:**
- Modify: `internal/cli/cli.go`
- Modify: `internal/cli/cli_test.go`

- [ ] **Step 1: 写失败测试（坏参数路径，不需真 claude）**

在 `internal/cli/cli_test.go` 追加：

```go
func TestRunChain_缺文件参数报错(t *testing.T) {
	if code := Execute([]string{"chain"}); code == 0 {
		t.Error("chain 缺文件应非 0")
	}
}

func TestRunChain_文件不存在报错(t *testing.T) {
	if code := Execute([]string{"chain", "no-such-file.yaml"}); code == 0 {
		t.Error("文件不存在应非 0")
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/cli/ -run TestRunChain`
Expected: FAIL（chain 落到解析路径，可能把 "chain" 当 profile 名）

- [ ] **Step 3: 接 cli**

在 switch 里 `case "doctor":` 之后加：

```go
		case "chain":
			return runChain(cfg, args[1:], os.Stdout)
```

文件末尾追加（import 加 `"os"` 已有；加 `"github.com/ttsimon/cc-run/internal/chain"`）：

```go
// runChain: ccr chain <file> [--auto]
func runChain(cfg config.Config, args []string, out io.Writer) int {
	auto := false
	var file string
	for _, a := range args {
		switch a {
		case "--auto":
			auto = true
		default:
			file = a
		}
	}
	if file == "" {
		fmt.Fprintln(os.Stderr, "用法: ccr chain <chain.yaml> [--auto]")
		return 1
	}
	data, err := os.ReadFile(file)
	if err != nil {
		fmt.Fprintln(os.Stderr, "读不到 chain 文件:", err)
		return 1
	}
	c, err := chain.Parse(data)
	if err != nil {
		fmt.Fprintln(os.Stderr, err)
		return 1
	}
	r, code := buildRegistry(cfg)
	if code != 0 {
		return code
	}
	o := chain.NewOrchestrator(r)
	o.Auto = auto
	if err := o.Run(c); err != nil {
		fmt.Fprintln(os.Stderr, "chain 失败:", err)
		return 1
	}
	fmt.Fprintln(out, "chain 完成:", c.Name)
	return 0
}
```

- [ ] **Step 4: 跑测试确认通过 + 全量 + 提交**

Run: `go test ./... && go build ./cmd/ccr`
Expected: PASS

```bash
git add internal/cli/cli.go internal/cli/cli_test.go
git commit -m "feat: add ccr chain command to run a pipeline

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

> **Phase B 里程碑**：此时 `ccr chain demo.yaml --auto` 已能用真 claude 顺序跑通一条线性链（规划→实现→审查），段间靠共享目录 + `{{prev.output}}` 交棒。建议在本机用一条真实小链冒烟（需装 claude）。

---

# Phase C — 人在环里（段间放行）

## Task C1: 放行决策 + Pauser 接口

**Files:**
- Create: `internal/chain/pause.go`
- Create: `internal/chain/pause_test.go`

- [ ] **Step 1: 写失败测试**

`internal/chain/pause_test.go`:

```go
package chain

import "testing"

func TestParseDecision(t *testing.T) {
	cases := map[string]Decision{
		"":  DecisionProceed, // 回车=放行
		"y": DecisionProceed,
		"s": DecisionSkip,
		"q": DecisionQuit,
		"e": DecisionEdit,
	}
	for in, want := range cases {
		if got := parseDecision(in); got != want {
			t.Errorf("parseDecision(%q)=%v want %v", in, got, want)
		}
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/chain/ -run TestParseDecision`
Expected: FAIL

- [ ] **Step 3: 写实现**

`internal/chain/pause.go`:

```go
package chain

import "strings"

// Decision 是放行点用户的选择。
type Decision int

const (
	DecisionProceed Decision = iota // 放行下一段
	DecisionSkip                    // 跳过下一段
	DecisionQuit                    // 退出整条链
	DecisionEdit                    // 改下一段的指令后再放行
)

// parseDecision 把用户输入映射为决策；空/y=放行。
func parseDecision(s string) Decision {
	switch strings.ToLower(strings.TrimSpace(s)) {
	case "s", "skip":
		return DecisionSkip
	case "q", "quit":
		return DecisionQuit
	case "e", "edit":
		return DecisionEdit
	default:
		return DecisionProceed
	}
}

// Pauser 在段间征询用户决策；prevOutput 是刚跑完那段的产出（供展示）。
// 返回决策；DecisionEdit 时第二个返回值是用户改写的新指令。
type Pauser interface {
	Pause(nextSeg Segment, prevOutput string) (Decision, string, error)
}
```

- [ ] **Step 4: 跑测试确认通过 + 提交**

Run: `go test ./internal/chain/ -run TestParseDecision`
Expected: PASS

```bash
git add internal/chain/pause.go internal/chain/pause_test.go
git commit -m "feat: add chain pause decision parsing and Pauser interface

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task C2: 编排器接入放行（非 --auto 时段间停顿）

**Files:**
- Modify: `internal/chain/orchestrate.go`
- Modify: `internal/chain/orchestrate_test.go`

- [ ] **Step 1: 写失败测试（注入假 Pauser）**

在 `orchestrate_test.go` 追加：

```go
// fakePauser 按预设序列返回决策。
type fakePauser struct {
	seq  []Decision
	edit string
	i    int
}

func (f *fakePauser) Pause(_ Segment, _ string) (Decision, string, error) {
	d := DecisionProceed
	if f.i < len(f.seq) {
		d = f.seq[f.i]
	}
	f.i++
	return d, f.edit, nil
}

func TestOrchestrate_退出在放行点中止(t *testing.T) {
	c := Chain{Segments: []Segment{
		{Name: "a", Profile: "strong", Prompt: "x"},
		{Name: "b", Profile: "cheap", Prompt: "y"},
	}}
	calls := 0
	o := NewOrchestrator(testReg())
	o.Auto = false
	o.Pauser = &fakePauser{seq: []Decision{DecisionQuit}} // 第一段后选退出
	o.runSegment = func(spec runSpec, seg Segment) (string, int, error) { calls++; return "out", 0, nil }
	if err := o.Run(c); err != nil {
		t.Fatal(err)
	}
	if calls != 1 {
		t.Errorf("退出后不应跑第二段, calls=%d", calls)
	}
}

func TestOrchestrate_跳过略过下一段(t *testing.T) {
	c := Chain{Segments: []Segment{
		{Name: "a", Profile: "strong", Prompt: "x"},
		{Name: "b", Profile: "cheap", Prompt: "y"},
		{Name: "c", Profile: "strong", Prompt: "z"},
	}}
	var ran []string
	o := NewOrchestrator(testReg())
	o.Auto = false
	o.Pauser = &fakePauser{seq: []Decision{DecisionSkip, DecisionProceed}} // a 后跳过 b
	o.runSegment = func(spec runSpec, seg Segment) (string, int, error) { ran = append(ran, seg.Name); return "o", 0, nil }
	if err := o.Run(c); err != nil {
		t.Fatal(err)
	}
	if strings.Join(ran, ",") != "a,c" {
		t.Errorf("应跑 a,c（跳过 b）, got %v", ran)
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/chain/ -run TestOrchestrate_退出在放行点中止`
Expected: FAIL（Orchestrator 无 Pauser 字段）

- [ ] **Step 3: 改编排器接入放行**

`orchestrate.go`：给 `Orchestrator` 加字段 `Pauser Pauser`，并把 `Run` 的循环改为「跑完一段后，若非 Auto 且还有下一段，则 Pause 征询」。完整替换 `Run`：

```go
// Run 顺序跑完整条链；非 Auto 时每段后在放行点征询用户。
func (o *Orchestrator) Run(c Chain) error {
	workdir := c.Workdir
	if workdir == "" {
		workdir = "."
	}
	var prev string
	for i := 0; i < len(c.Segments); i++ {
		seg := c.Segments[i]

		p, err := o.reg.Resolve(seg.Profile)
		if err != nil {
			return fmt.Errorf("段 #%d(%q) 的 profile 解析失败: %w", i, seg.Name, err)
		}
		spec := runSpec{
			Prompt:     Render(seg.Prompt, prev),
			AllowTools: seg.AllowTools,
			Workdir:    workdir,
			Env:        p.Env,
		}
		out, code, err := o.runSegment(spec, seg)
		if err != nil {
			return fmt.Errorf("段 #%d(%q) 启动失败: %w", i, seg.Name, err)
		}
		if code != 0 {
			return fmt.Errorf("段 #%d(%q) 非 0 退出（%d），中止", i, seg.Name, code)
		}
		prev = out

		// 放行点（非 Auto，且后面还有段）
		if !o.Auto && i+1 < len(c.Segments) {
			next := c.Segments[i+1]
			d, edited, perr := o.Pauser.Pause(next, prev)
			if perr != nil {
				return perr
			}
			switch d {
			case DecisionQuit:
				return nil
			case DecisionSkip:
				i++ // 跳过下一段
			case DecisionEdit:
				if edited != "" {
					c.Segments[i+1].Prompt = edited
				}
			case DecisionProceed:
			}
		}
	}
	return nil
}
```

> 注：`Skip` 用 `i++` 跳过下一段（再由 for 的 `i++` 推进）。`Edit` 改写下一段 prompt（改的是本次运行的副本 `c`，因 `c Chain` 是值传入、Segments 切片元素可改）。

- [ ] **Step 4: 跑测试确认通过**

Run: `go test ./internal/chain/`
Expected: PASS（含 Phase B 的 Auto 用例仍绿）

- [ ] **Step 5: 写终端 Pauser 实现（手动验证，非单测）**

在 `pause.go` 追加一个基于 stdin 的实现（真实运行用；TUI 可后续替换）：

```go
import (
	"bufio"
	"fmt"
	"io"
	"os"
)

// TermPauser 在终端打印决策提示并读一行。
type TermPauser struct {
	In  io.Reader
	Out io.Writer
}

// NewTermPauser 默认接 os.Stdin/os.Stdout。
func NewTermPauser() *TermPauser { return &TermPauser{In: os.Stdin, Out: os.Stdout} }

func (t *TermPauser) Pause(nextSeg Segment, prevOutput string) (Decision, string, error) {
	fmt.Fprintf(t.Out, "\n⏸  上段产出：\n%s\n", prevOutput)
	fmt.Fprintf(t.Out, "下一段：%s（profile=%s）\n", nextSeg.Name, nextSeg.Profile)
	fmt.Fprint(t.Out, "[回车=放行 / s=跳过 / e=改指令 / q=退出] > ")
	line, _ := bufio.NewReader(t.In).ReadString('\n')
	d := parseDecision(line)
	if d == DecisionEdit {
		fmt.Fprint(t.Out, "输入新指令（单行）> ")
		edited, _ := bufio.NewReader(t.In).ReadString('\n')
		return d, edited, nil
	}
	return d, "", nil
}
```

并在 `NewOrchestrator` 里把默认 Pauser 设上：`o.Pauser = NewTermPauser()`。

- [ ] **Step 6: 构建 + 提交**

Run: `go test ./... && go build ./cmd/ccr`
Expected: PASS

```bash
git add internal/chain/orchestrate.go internal/chain/orchestrate_test.go internal/chain/pause.go
git commit -m "feat: pause between chain segments for proceed/skip/edit/quit

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

# Phase D — 审查段：findings + 判定 + 可选修复段

## Task D1: 判定文件约定 + 给审查段追加产出指令

**Files:**
- Create: `internal/chain/verdict.go`
- Create: `internal/chain/verdict_test.go`
- Modify: `internal/chain/orchestrate.go`

**约定**：审查段（`review: true`）在工作目录写两份文件——`.ccr-chain/findings.md`（问题列表）与 `.ccr-chain/verdict`（单行 `pass` 或 `needs-work`）。编排器在 `review` 段跑完后读 `verdict`。为了让审查 claude 知道要写，编排器给该段 prompt **追加一段固定指令**。

- [ ] **Step 1: 写失败测试**

`internal/chain/verdict_test.go`:

```go
package chain

import (
	"os"
	"path/filepath"
	"testing"
)

func TestReadVerdict(t *testing.T) {
	dir := t.TempDir()
	vdir := filepath.Join(dir, ".ccr-chain")
	os.MkdirAll(vdir, 0o755)
	os.WriteFile(filepath.Join(vdir, "verdict"), []byte("needs-work\n"), 0o644)
	v := ReadVerdict(dir)
	if v != VerdictNeedsWork {
		t.Errorf("got %v", v)
	}
}

func TestReadVerdict_缺文件为未知(t *testing.T) {
	if ReadVerdict(t.TempDir()) != VerdictUnknown {
		t.Error("缺 verdict 文件应为 Unknown")
	}
}

func TestReviewInstruction_含文件路径(t *testing.T) {
	s := ReviewInstruction()
	if !strings.Contains(s, ".ccr-chain/verdict") || !strings.Contains(s, ".ccr-chain/findings.md") {
		t.Errorf("追加指令应告知写两份文件: %q", s)
	}
}
```
（文件顶部 import 需要 `"strings"`。）

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/chain/ -run 'Verdict|ReviewInstruction'`
Expected: FAIL

- [ ] **Step 3: 写实现**

`internal/chain/verdict.go`:

```go
package chain

import (
	"os"
	"path/filepath"
	"strings"
)

// Verdict 是审查段的判定。
type Verdict int

const (
	VerdictUnknown Verdict = iota
	VerdictPass
	VerdictNeedsWork
)

// ReadVerdict 读 workdir/.ccr-chain/verdict；缺文件或无法识别为 Unknown。
func ReadVerdict(workdir string) Verdict {
	raw, err := os.ReadFile(filepath.Join(workdir, ".ccr-chain", "verdict"))
	if err != nil {
		return VerdictUnknown
	}
	switch strings.TrimSpace(string(raw)) {
	case "pass":
		return VerdictPass
	case "needs-work":
		return VerdictNeedsWork
	default:
		return VerdictUnknown
	}
}

// ReviewInstruction 是追加到审查段 prompt 末尾的固定指令。
func ReviewInstruction() string {
	return "\n\n---\n审查完成后，必须：1) 把问题清单写到 .ccr-chain/findings.md；" +
		"2) 在 .ccr-chain/verdict 写单独一行，内容为 pass 或 needs-work。"
}
```

- [ ] **Step 4: 编排器：review 段追加指令、跑完读判定（失败测试）**

在 `orchestrate_test.go` 追加：

```go
func TestOrchestrate_review段追加指令(t *testing.T) {
	c := Chain{Segments: []Segment{{Name: "r", Profile: "strong", Prompt: "审查", Review: true}}}
	var seen string
	o := NewOrchestrator(testReg())
	o.Auto = true
	o.runSegment = func(spec runSpec, seg Segment) (string, int, error) { seen = spec.Prompt; return "o", 0, nil }
	if err := o.Run(c); err != nil {
		t.Fatal(err)
	}
	if !strings.Contains(seen, ".ccr-chain/verdict") {
		t.Errorf("review 段 prompt 应被追加判定指令: %q", seen)
	}
}
```

- [ ] **Step 5: 在编排器渲染 prompt 后、对 review 段追加指令**

`orchestrate.go` 的 `Run` 里，构造 `spec.Prompt` 处改为：

```go
		renderedPrompt := Render(seg.Prompt, prev)
		if seg.Review {
			renderedPrompt += ReviewInstruction()
		}
		spec := runSpec{
			Prompt:     renderedPrompt,
			AllowTools: seg.AllowTools,
			Workdir:    workdir,
			Env:        p.Env,
		}
```

- [ ] **Step 6: 跑测试确认通过 + 提交**

Run: `go test ./internal/chain/`
Expected: PASS

```bash
git add internal/chain/verdict.go internal/chain/verdict_test.go internal/chain/orchestrate.go
git commit -m "feat: review segment writes verdict/findings; orchestrator reads verdict

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task D2: 可选修复段 —— 按判定在放行点给默认建议

**Files:**
- Modify: `internal/chain/orchestrate.go`
- Modify: `internal/chain/orchestrate_test.go`

**规则**：标了 `optional: true` 的段（如修复段），在 `--auto` 下**正常跑**（一条道跑到黑）；在非 auto 下，**只有用户在放行点选放行才跑**，否则按跳过处理。判定（pass/needs-work）作为放行点展示信息提示用户，但**不自动分叉**（保持 YAGNI，不造条件执行引擎）。

- [ ] **Step 1: 写失败测试**

```go
func TestOrchestrate_可选段auto下照跑(t *testing.T) {
	c := Chain{Segments: []Segment{
		{Name: "impl", Profile: "cheap", Prompt: "x"},
		{Name: "fix", Profile: "cheap", Prompt: "y", Optional: true},
	}}
	var ran []string
	o := NewOrchestrator(testReg())
	o.Auto = true
	o.runSegment = func(spec runSpec, seg Segment) (string, int, error) { ran = append(ran, seg.Name); return "o", 0, nil }
	o.Run(c)
	if strings.Join(ran, ",") != "impl,fix" {
		t.Errorf("auto 下可选段应照跑: %v", ran)
	}
}

func TestOrchestrate_可选段非auto默认跳过(t *testing.T) {
	c := Chain{Segments: []Segment{
		{Name: "impl", Profile: "cheap", Prompt: "x"},
		{Name: "fix", Profile: "cheap", Prompt: "y", Optional: true},
	}}
	var ran []string
	o := NewOrchestrator(testReg())
	o.Auto = false
	o.Pauser = &fakePauser{seq: []Decision{DecisionSkip}} // 放行点选跳过修复段
	o.runSegment = func(spec runSpec, seg Segment) (string, int, error) { ran = append(ran, seg.Name); return "o", 0, nil }
	o.Run(c)
	if strings.Join(ran, ",") != "impl" {
		t.Errorf("非 auto 跳过可选段后只应跑 impl: %v", ran)
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/chain/ -run 可选段`
Expected: FAIL（auto 下可选段已会跑——第一个用例可能已过；第二个验证跳过逻辑已由 C2 的 Skip 覆盖）。若两用例都已意外通过，说明 C2 的 Skip 已足够——此时本任务只需补「放行点展示判定」即可（见 Step 3）。

> 设计澄清：`optional` 段的「非 auto 默认跳过」其实由 C2 的放行机制天然支持（用户在 fix 段前的放行点选 s 即跳过）。本任务**不需要新分支逻辑**，重点是 Step 3：让放行点把上一段的判定（若有）显示出来，帮用户决定对可选段放行还是跳过。

- [ ] **Step 3: 放行点展示判定**

`orchestrate.go`：在放行前读判定并传给 Pauser 展示。把 `Pauser.Pause` 的签名扩展为带判定，或更简单——在 `prev` 输出后追加一行判定摘要。采用后者（最小改动）：放行前，若**刚跑完的段**是 review 段，则读 `ReadVerdict(workdir)` 并把结果拼到展示文本：

```go
		// 放行点（非 Auto，且后面还有段）
		if !o.Auto && i+1 < len(c.Segments) {
			info := prev
			if seg.Review {
				switch ReadVerdict(workdir) {
				case VerdictPass:
					info += "\n[判定] pass ✓"
				case VerdictNeedsWork:
					info += "\n[判定] needs-work —— 下一段建议放行修复"
				}
			}
			next := c.Segments[i+1]
			d, edited, perr := o.Pauser.Pause(next, info)
			// ...（其余分支同 C2）
		}
```

- [ ] **Step 4: 跑测试确认通过 + 提交**

Run: `go test ./internal/chain/`
Expected: PASS

```bash
git add internal/chain/orchestrate.go internal/chain/orchestrate_test.go
git commit -m "feat: surface review verdict at pause for optional fix segment

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

# Phase E — 安全：worktree 隔离 + 命令黑名单钩子

## Task E1: git worktree 隔离的建/拆

**Files:**
- Create: `internal/chain/worktree.go`
- Create: `internal/chain/worktree_test.go`

- [ ] **Step 1: 写失败测试（用真 git，在 t.TempDir 里 init 一个仓库）**

`internal/chain/worktree_test.go`:

```go
package chain

import (
	"os"
	"os/exec"
	"path/filepath"
	"testing"
)

func initRepo(t *testing.T) string {
	t.Helper()
	dir := t.TempDir()
	for _, args := range [][]string{
		{"init"}, {"config", "user.email", "t@t"}, {"config", "user.name", "t"},
	} {
		cmd := exec.Command("git", args...)
		cmd.Dir = dir
		if out, err := cmd.CombinedOutput(); err != nil {
			t.Fatalf("git %v: %v %s", args, err, out)
		}
	}
	os.WriteFile(filepath.Join(dir, "f.txt"), []byte("hi"), 0o644)
	for _, args := range [][]string{{"add", "."}, {"commit", "-m", "init"}} {
		cmd := exec.Command("git", args...)
		cmd.Dir = dir
		if out, err := cmd.CombinedOutput(); err != nil {
			t.Fatalf("git %v: %v %s", args, err, out)
		}
	}
	return dir
}

func TestWorktree_建后存在拆后消失(t *testing.T) {
	repo := initRepo(t)
	wt, cleanup, err := CreateWorktree(repo, "ccr-chain-test")
	if err != nil {
		t.Fatal(err)
	}
	if _, err := os.Stat(filepath.Join(wt, "f.txt")); err != nil {
		t.Errorf("worktree 里应有仓库文件: %v", err)
	}
	cleanup()
	if _, err := os.Stat(wt); !os.IsNotExist(err) {
		t.Errorf("cleanup 后 worktree 应被移除")
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/chain/ -run TestWorktree`
Expected: FAIL

- [ ] **Step 3: 写实现**

`internal/chain/worktree.go`:

```go
package chain

import (
	"fmt"
	"os"
	"os/exec"
	"path/filepath"
	"time"
)

// CreateWorktree 在 repo 上开一个临时 git worktree（新分支），返回路径与清理函数。
// 链跑完/出错都应调 cleanup：移除 worktree 并删临时分支，原仓库不受影响。
func CreateWorktree(repo, label string) (dir string, cleanup func(), err error) {
	branch := fmt.Sprintf("ccr-chain/%s-%d", label, time.Now().Unix())
	dir = filepath.Join(os.TempDir(), "ccr-chain-"+fmt.Sprint(time.Now().UnixNano()))

	if out, e := gitIn(repo, "worktree", "add", "-b", branch, dir); e != nil {
		return "", nil, fmt.Errorf("建 worktree 失败: %v %s", e, out)
	}
	cleanup = func() {
		_, _ = gitIn(repo, "worktree", "remove", "--force", dir)
		_, _ = gitIn(repo, "branch", "-D", branch)
	}
	return dir, cleanup, nil
}

func gitIn(dir string, args ...string) ([]byte, error) {
	cmd := exec.Command("git", args...)
	cmd.Dir = dir
	return cmd.CombinedOutput()
}
```

- [ ] **Step 4: 跑测试确认通过 + 提交**

Run: `go test ./internal/chain/ -run TestWorktree`
Expected: PASS

```bash
git add internal/chain/worktree.go internal/chain/worktree_test.go
git commit -m "feat: create/cleanup isolated git worktree for chain runs

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

- [ ] **Step 5: 编排器在 isolate=true 时跑在 worktree（失败测试 + 接线）**

在 `orchestrate_test.go` 加一个用例：`Chain{Isolate:true,...}` 跑时 `runSegment` 收到的 `spec.Workdir` 指向一个存在的临时目录（用桩断言 workdir != "." 且存在）。然后在 `orchestrate.go` 的 `Run` 开头：若 `c.Isolate`，调 `CreateWorktree`（repo 取 `c.Workdir` 或当前目录），`defer cleanup()`，把 `workdir` 指向新建目录。注意：isolate 需要当前目录是 git 仓库，否则报错提示。
> 完整接线代码：在 `Run` 里 `workdir` 计算之后插入：
```go
	if c.Isolate {
		wt, cleanup, err := CreateWorktree(workdir, sanitize(c.Name))
		if err != nil {
			return fmt.Errorf("隔离 worktree 失败（当前目录需为 git 仓库）: %w", err)
		}
		defer cleanup()
		workdir = wt
	}
```
其中 `sanitize` 把 name 里的非字母数字换成 `-`（写个 5 行小函数 + 单测）。

- [ ] **Step 6: 跑测试 + 提交**

Run: `go test ./internal/chain/`
Expected: PASS

```bash
git add internal/chain/
git commit -m "feat: run chain in isolated worktree when isolate is set

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task E2: 命令黑名单 + PreToolUse 钩子 + `ccr __chain_guard`

**Files:**
- Create: `internal/chain/security.go`
- Create: `internal/chain/security_test.go`
- Modify: `internal/cli/cli.go`（加隐藏 `__chain_guard`）
- Modify: `internal/chain/orchestrate.go`（生成 settings.json 传给段）

**机制**：每段跑前生成一个临时 `settings.json`，内含 PreToolUse 钩子，钩子命令是 `ccr __chain_guard`。claude 执行 Bash 工具前会把工具输入（JSON）经 stdin 传给钩子；`ccr __chain_guard` 读 stdin + 环境变量 `CCR_CHAIN_DENY`（换行分隔的黑名单），命中则以**退出码 2**阻止该工具调用。黑名单 = 内置默认 + 段的 `deny_commands`。

- [ ] **Step 1: 写失败测试（黑名单匹配 + 默认项）**

`internal/chain/security_test.go`:

```go
package chain

import (
	"strings"
	"testing"
)

func TestDenied_命中内置默认(t *testing.T) {
	bl := DefaultDenylist()
	if !Denied("rm -rf /", bl) {
		t.Error("rm -rf 应被默认黑名单拦")
	}
	if Denied("ls -la", bl) {
		t.Error("普通命令不应被拦")
	}
}

func TestDenied_合并段追加(t *testing.T) {
	bl := MergeDenylist(DefaultDenylist(), []string{"curl evil.com"})
	if !Denied("curl evil.com --data x", bl) {
		t.Error("段追加项应生效")
	}
}

func TestSettingsJSON_含PreToolUse与guard(t *testing.T) {
	s := SettingsJSON("ccr")
	if !strings.Contains(s, "PreToolUse") || !strings.Contains(s, "__chain_guard") {
		t.Errorf("settings 应含 PreToolUse 钩子调 __chain_guard: %s", s)
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `go test ./internal/chain/ -run 'Denied|Settings'`
Expected: FAIL

- [ ] **Step 3: 写实现**

`internal/chain/security.go`:

```go
package chain

import (
	"fmt"
	"strings"
)

// DefaultDenylist 是 ccr 内置的命令红线（子串匹配，命中即拦）。
func DefaultDenylist() []string {
	return []string{
		"rm -rf",
		"git push",
		"shutdown",
		"mkfs",
		":(){ :|:& };:", // fork bomb
		"dd if=",
		"> /dev/sd",
	}
}

// MergeDenylist 合并默认与用户追加。
func MergeDenylist(base, extra []string) []string {
	return append(append([]string{}, base...), extra...)
}

// Denied 报告 cmd 是否命中黑名单（任一项为其子串）。
func Denied(cmd string, denylist []string) bool {
	for _, d := range denylist {
		if d != "" && strings.Contains(cmd, d) {
			return true
		}
	}
	return false
}

// SettingsJSON 生成传给 claude --settings 的内容：一个 PreToolUse 钩子，
// 在 Bash 工具前调用 `<ccrPath> __chain_guard`。
func SettingsJSON(ccrPath string) string {
	return fmt.Sprintf(`{
  "hooks": {
    "PreToolUse": [
      { "matcher": "Bash", "hooks": [ { "type": "command", "command": %q } ] }
    ]
  }
}`, ccrPath+" __chain_guard")
}
```
> ⚠️ 集成边界：PreToolUse 钩子的 settings 结构、stdin 输入字段名、用退出码 2 阻止——按当前 Claude Code 钩子协议编写。实现时对照 `claude` 文档/`--help` 与一次真实试跑确认字段名（尤其工具输入里 Bash 命令的 JSON 路径），不符则改 `__chain_guard` 的解析与本 JSON 结构。

- [ ] **Step 4: 实现 `ccr __chain_guard`（读 stdin + env，命中退 2）**

先写它的纯逻辑（可测）：在 `security.go` 加一个从 JSON 输入里取 Bash 命令的函数：

```go
import "encoding/json"

// hookInput 是 PreToolUse 钩子从 stdin 收到的（按 Claude Code 协议，字段名待集成时核对）。
type hookInput struct {
	ToolName  string `json:"tool_name"`
	ToolInput struct {
		Command string `json:"command"`
	} `json:"tool_input"`
}

// CommandFromHookInput 从钩子 stdin JSON 里取出 Bash 命令；解析失败返回空串。
func CommandFromHookInput(raw []byte) string {
	var in hookInput
	if json.Unmarshal(raw, &in) != nil {
		return ""
	}
	return in.ToolInput.Command
}
```

加测试 `TestCommandFromHookInput`（喂 `{"tool_name":"Bash","tool_input":{"command":"rm -rf /"}}` 期望取出 `rm -rf /`）。

然后在 `internal/cli/cli.go` 加隐藏命令：switch 里 `case "__chain_guard": return runChainGuard(os.Stdin, os.Stderr)`，实现：

```go
// runChainGuard: PreToolUse 钩子调用；命中黑名单（CCR_CHAIN_DENY 换行分隔）则退 2 阻止。
func runChainGuard(in io.Reader, errOut io.Writer) int {
	raw, _ := io.ReadAll(in)
	cmd := chain.CommandFromHookInput(raw)
	denylist := strings.Split(os.Getenv("CCR_CHAIN_DENY"), "\n")
	if chain.Denied(cmd, denylist) {
		fmt.Fprintf(errOut, "ccr: 命令命中红线黑名单，已拦截：%s\n", cmd)
		return 2
	}
	return 0
}
```
（import 加 `"strings"`、`"io"`、`chain`。）

- [ ] **Step 5: 编排器：每段生成 settings + 传 CCR_CHAIN_DENY**

`orchestrate.go`：跑每段前，把 `SettingsJSON(ccrPath)` 写到 `workdir/.ccr-chain/settings-<seg>.json`，`spec.SettingsPath` 指向它；并把合并后的黑名单经 env `CCR_CHAIN_DENY`（换行 join）塞进 `spec.Env`。`ccrPath` 取 `os.Executable()`（失败回退字符串 `"ccr"`）。给 `runSpec` 已有 `Env`/`SettingsPath` 字段赋值即可。加一个用例断言：跑 review/普通段时 `spec.SettingsPath` 非空且 `spec.Env["CCR_CHAIN_DENY"]` 含默认项。

- [ ] **Step 6: 跑测试 + 全量 + 提交**

Run: `go test ./... && go build ./cmd/ccr`
Expected: PASS

```bash
git add internal/chain/ internal/cli/cli.go
git commit -m "feat: PreToolUse denylist guard for chain segments

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

> **集成验证（手动，需真 claude）**：写一条 deny_commands 含 `rm -rf` 的链，让实现段尝试 `rm -rf` 某文件，确认被 `ccr __chain_guard` 拦下（claude 报工具被 PreToolUse 阻止）。若钩子字段名/退出码语义与实测不符，按 Task E2 的集成边界提示改 `hookInput` 与 `SettingsJSON`。

---

# Phase F — 作者体验 + 文档

## Task F1: `ccr chain init` + 内置模板

**Files:**
- Create: `internal/chain/templates/plan-impl-review.yaml`
- Create: `internal/chain/templates.go`（go:embed）
- Create: `internal/chain/templates_test.go`
- Modify: `internal/cli/cli.go`

- [ ] **Step 1: 写内置模板**

`internal/chain/templates/plan-impl-review.yaml`（一条可直接改用的规划→实现→审查链；profile 用占位提示用户改）:

```yaml
name: plan-impl-review
isolate: true
segments:
  - name: plan
    profile: 改成你的强模型profile名    # 如 cc-switch:opus
    prompt: |
      把目标拆成一份结构化计划写到 docs/plans/目标.md：
      每步写清楚改哪个文件、做什么、怎么验证（阶段/任务/验证三件套）。
    allow_tools: [Read, Write, Edit]
  - name: implement
    profile: 改成你的便宜模型profile名   # 如 custom:cheap
    prompt: |
      读 {{prev.output}} 指向的计划文件，逐步实现。严格照计划做，不要自行扩展。
    allow_tools: [Read, Write, Edit, Bash]
    deny_commands: ["git push"]
  - name: review
    profile: 改成另一家profile名
    prompt: |
      审查本次改动是否符合计划、有无明显 bug。
    review: true
    allow_tools: [Read, Bash]
```

- [ ] **Step 2: 写失败测试**

`internal/chain/templates_test.go`:

```go
package chain

import (
	"strings"
	"testing"
)

func TestTemplate_存在且可解析(t *testing.T) {
	raw, ok := Template("plan-impl-review")
	if !ok {
		t.Fatal("内置模板应存在")
	}
	if _, err := Parse([]byte(raw)); err != nil {
		// 模板里 profile 是占位中文，但 Validate 只查非空，应能过
		t.Fatalf("内置模板应能解析: %v", err)
	}
	if !strings.Contains(raw, "{{prev.output}}") {
		t.Error("模板应演示 prev.output 交棒")
	}
}

func TestTemplate_未知名返回false(t *testing.T) {
	if _, ok := Template("nope"); ok {
		t.Error("未知模板应返回 false")
	}
}
```

- [ ] **Step 3: 跑测试确认失败**

Run: `go test ./internal/chain/ -run TestTemplate`
Expected: FAIL

- [ ] **Step 4: 写实现（go:embed）**

`internal/chain/templates.go`:

```go
package chain

import (
	"embed"
	"io/fs"
)

//go:embed templates/*.yaml
var templatesFS embed.FS

// Template 按名返回内置 chain 模板内容；不存在返回 ok=false。
func Template(name string) (string, bool) {
	raw, err := fs.ReadFile(templatesFS, "templates/"+name+".yaml")
	if err != nil {
		return "", false
	}
	return string(raw), true
}
```

- [ ] **Step 5: 接 cli（`ccr chain init [模板名]` 写出文件）**

`runChain` 里在解析 `file` 之前先判 `args[0] == "init"`：

```go
	if len(args) > 0 && args[0] == "init" {
		name := "plan-impl-review"
		if len(args) > 1 {
			name = args[1]
		}
		raw, ok := chain.Template(name)
		if !ok {
			fmt.Fprintf(os.Stderr, "没有内置模板 %q（现有：plan-impl-review）\n", name)
			return 1
		}
		dest := name + ".chain.yaml"
		if _, err := os.Stat(dest); err == nil {
			fmt.Fprintf(os.Stderr, "%s 已存在，不覆盖\n", dest)
			return 1
		}
		if err := os.WriteFile(dest, []byte(raw), 0o644); err != nil {
			fmt.Fprintln(os.Stderr, err)
			return 1
		}
		fmt.Fprintf(out, "已生成 %s，改好里面的 profile 名后用 `ccr chain %s` 跑\n", dest, dest)
		return 0
	}
```

- [ ] **Step 6: 跑测试 + 冒烟 + 提交**

Run: `go test ./... && go build ./cmd/ccr && go run ./cmd/ccr chain init`
Expected: 生成 `plan-impl-review.chain.yaml`。

```bash
git add internal/chain/ internal/cli/cli.go
git commit -m "feat: ccr chain init scaffolds a chain from built-in template

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task F2: 帮助文本 + README + CLAUDE.md

**Files:**
- Modify: `internal/cli/cli.go`（printUsage 加 chain/doctor）
- Modify: `README.md`
- Modify: `CLAUDE.md`

- [ ] **Step 1: 更新 printUsage**

在 `printUsage` 的命令列表里补两行（保持现有风格）：

```
  ccr doctor [名字]            体检后端可达性（不带名=全部）
  ccr chain <file> [--auto]    跑一条多后端流水线；ccr chain init 生成模板
```

- [ ] **Step 2: README 加一节**

在 README 加 `## 多后端流水线（chain）` 一节：说明 `ccr chain init` → 改 profile → `ccr chain x.chain.yaml`；解释段间放行（回车/s/e/q）、`--auto`、`isolate` worktree、`review` 段与 deny_commands 安全。给出 `plan-impl-review` 的简述。

- [ ] **Step 3: CLAUDE.md 更新**

代码布局加 `internal/chain/`、`internal/doctor/`；架构补一句：chain = 多后端流水线（无头 claude -p 分段 + 共享 worktree 交棒 + 放行/审查/黑名单钩子）。

- [ ] **Step 4: 提交**

```bash
git add internal/cli/cli.go README.md CLAUDE.md
git commit -m "docs: document ccr chain and doctor

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 收尾

- 全部完成后 `go test ./...` + `go vet ./...` + `gofmt -l .` 全绿；开分支 → PR（master PR-only）。
- chain 是 v0.3，发版按 `RELEASING.md` 打 tag。
- **端到端真 claude 验证清单**（发版前手动跑一遍）：①一条线性链 `--auto` 跑通；②非 auto 放行/跳过/改指令/退出四种决策；③review 段写出 verdict 并在放行点显示；④isolate worktree 跑完被清理、原仓库干净；⑤deny_commands 命中被 `ccr __chain_guard` 拦。

---

## 集成边界汇总（实现时务必逐条对照安装的 claude 版本）

1. **段执行旗标**（Task B3/B4）：`-p` / `--allowedTools` / `--add-dir` / `--output-format text` / `--settings`。跑 `claude --help` 核对。
2. **headless 输出**：`--output-format text` 下，claude 把回答打到 stdout——ccr 捕获它作为 `{{prev.output}}`。若需要结构化，可改 `stream-json` 并解析（YAGNI，先用 text）。
3. **PreToolUse 钩子**（Task E2）：settings.json 结构、stdin 输入 JSON 的字段名（`tool_name` / `tool_input.command`）、用退出码 2 阻止——对照 Claude Code 钩子协议核对，不符则改 `SettingsJSON` 与 `hookInput`。
4. **权限模式**：headless 跑工具可能需要 `--permission-mode`（如 `acceptEdits`）才不卡在批准上。集成时若发现段卡住，给 `SegmentArgs` 补 `--permission-mode acceptEdits` 并加测试。
